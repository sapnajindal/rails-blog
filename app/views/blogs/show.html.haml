%p
  %strong Title:
  = @blog.title
%p
  %strong Text:
  = @blog.text
- if @error && @error.any?
  #error_explanation
    %h2
      This comment Is not saved because :
    %ul
      = @error

%h2 Comments
= render @blog.comments
%h2 Add a comment:
= render 'comments/form', blog: @blog
= link_to 'Edit', edit_blog_path(@blog)
= link_to 'Back', blogs_path

